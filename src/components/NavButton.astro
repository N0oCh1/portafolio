---
const {
    isSelected,
    id,
    ref
} = Astro.props;
---

<button 
    id={id}
    class={isSelected ? 'bg-blue-500 text-white' : 'bg-gray-200 text-gray-700 hover:bg-gray-300' } >
    {ref}
    <slot />
</button>

